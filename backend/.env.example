# Fuente de datos: "sample" (offline, datos de ejemplo) o "api" (worldcup26.ir real)
DATA_SOURCE=api

# Credenciales worldcup26.ir (se registra solo la primera vez)
WC_EMAIL=tu_email@example.com
WC_PASSWORD=tu_password
WC_NAME=Predictor

# Cantidad de simulaciones Monte Carlo por corrida
SIMULATIONS=20000
